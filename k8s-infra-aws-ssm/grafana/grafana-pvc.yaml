apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: grafana-data
  namespace: dev
spec:
  storageClassName: gp3
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
