apiVersion: v1
kind: Service
metadata:
  name: loki-receiver-nlb
  namespace: monitor
spec:
  type: NodePort
  selector:
    app: loki
  ports:
    - name: http
      port: 3100
      targetPort: 3100
      nodePort: 30471
      protocol: TCP
