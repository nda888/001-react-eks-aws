apiVersion: v1
kind: Service
metadata:
  name: prometheus-receiver-nlb
  namespace: monitor
spec:
  type: NodePort
  selector:
    app: prometheus
  ports:
    - name: receiver
      port: 9090
      targetPort: 9090
      nodePort: 30503
      protocol: TCP
