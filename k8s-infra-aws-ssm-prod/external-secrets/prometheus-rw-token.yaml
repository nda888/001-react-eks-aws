apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: prometheus-rw-token
  namespace: prod
spec:
  refreshInterval: 5m
  secretStoreRef:
    name: aws-ssm
    kind: ClusterSecretStore
  target:
    name: prometheus-rw-token
    creationPolicy: Owner
  data:
    - secretKey: token
      remoteRef:
        key: /demo-eks-prod/monitor/prometheus_rw_token
